// ponytail: no command surface yet — validate and generate arrive with E13-05 and E17-02.
// Ceiling: this tool does nothing and exits 0, so wiring it into CI now would prove nothing.
// Upgrade: E13-05 adds `validate`; E13-06 is the story that makes CI call it.
return 0;
